# Test-quality remediation — 2026-09-09

Companion to `../test-quality-audit-2026-09-09.ranked.tsv` and
`../testing-best-practices.md`. Scope: every TypeScript/JavaScript row graded
C, D or F in the ranked TSV that still existed after the delete list was
applied (93 files). The nine Haskell rows (`offchain/tests/**`,
`onchain/plutarch/tests/**`) were left untouched by owner instruction; their
fate is the "wire it or retire it" owner call recorded in the delete list.

Each `batch-NN.md` is the per-file review-gate record the best-practices
doc's review gate asks for: contract, what changed, the failure-evidence
mutation and the assertion it tripped, verification commands and results,
and anything skipped. Batches were partitioned by package:

| Batch | Package(s)                                           | Files | Outcome                                                                                                                                              |
| ----- | ---------------------------------------------------- | ----- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| 01    | midgard-fault-proofs                                 | 9     | 7 edited, 2 already compliant, 1 partial (direct-frontier-exact stage literals need the fit-ledger generator; the recorded 14,058 frontier is stale) |
| 02    | midgard-fault-proofs                                 | 9     | 9 done; phase-A item ledger writer deleted (no consumer)                                                                                             |
| 03    | midgard-fault-proofs                                 | 9     | 9 done; new `tests/support/blueprint-abi.ts`                                                                                                         |
| 04    | midgard-fault-proofs                                 | 9     | 9 done; central-journal shared-suite promotion and CEK ledger-writer relocation deferred (cross-batch files)                                         |
| 05    | midgard-node                                         | 10    | 10 done; transition-trace ABI golden now generated by a checked-in script, `--check` in CI, consumed by a new Aiken golden test                      |
| 06    | midgard-node                                         | 9     | 8 rewritten, 1 kept per row                                                                                                                          |
| 07    | midgard-sdk                                          | 10    | 9 done, 1 partial (da-attestation emulator drive needs a harness the SDK lacks); stale `fault-proof.test.ts` allowlist refreshed                     |
| 08    | midgard-core, midgard-validation                     | 11    | 11 done; new nested-boundary Aiken golden channel with CI `--check`                                                                                  |
| 09    | midgard-watcher, da-committee-node                   | 9     | 9 done; `public-retained-da-store` now runs against real Postgres                                                                                    |
| 10    | da-committee-node, lucid-midgard, midgard-node-tools | 8     | 8 done; source-grep tests replaced by eslint `no-restricted-syntax` rules                                                                            |

## Non-test changes made in support of the tests

- `.github/workflows/midgard-node-ci.yml`: fail-closed `--check` steps for the
  transition-trace ABI golden, the validation-auxiliary-witness fixture, and the
  nested-boundary Aiken goldens.
- `demo/eslint.config.mjs`: `no-restricted-syntax` / `no-restricted-imports`
  rules that replace deleted source-text tests (lucid-midgard defect
  swallowing, example provider identifiers, watcher package boundary).
- Generators: `demo/midgard-node/scripts/generate-transition-trace-abi-fixture.mjs`,
  `demo/midgard-validation/scripts/generate-nested-boundary-aiken-goldens.mjs`,
  and the repaired `generate-validation-auxiliary-witness-v1-fixture.mjs`.
- Aiken: `onchain/aiken/validators/fraud-proofs/transition-trace/abi-v1-golden.test.ak`
  (generated) and hoisted generated constants in two nested-boundary modules.
- Production `src/` edits, all row-mandated and additive: an injected,
  defaulted clock in `demo/midgard-sdk/src/user-events/internals.ts`; a
  `reason` field on DA-attestation build failures in
  `demo/midgard-sdk/src/da-attestation.ts`.

## Open items for the owner

1. `submit-init-emulator-option-b-direct-frontier-exact`: the +1 refusal at
   item 14,059 no longer trips (pre-existing red); the frontier has moved
   outward and needs re-measuring. The five stage literals still need the
   fit-ledger generator.
2. `remove-fraudulent-block`: the row asks for conformance against
   midgard-node's real `/stateQueueMutationLease` handler. That test cannot
   live in midgard-fault-proofs (dependency cycle); it belongs in
   `demo/midgard-node/tests/`.
3. `da-attestation`: driving the builders through the Emulator needs real
   DA/state-queue/availability validators and a fault-proofs-scale harness.
4. `coordinator-factory`: the row's typed preflight-result parameter was not
   applied because preflight is legitimately optional.
5. The consensus release-gate refusal scenario cannot be rewritten:
   `assertMidgardConsensusReleaseReady` no longer exists in the tree.
6. `sdk-abi-fixtures`: ~40 round-trip-only cases were left as-is (scope call).
7. The cek-context fixture's Aiken-side generator is still owed.
8. Batch 01's "memory flat in cardinality" property has no rule-3 mutation
   (would need an Aiken edit).

## Pre-existing reds observed (not introduced here)

- `midgard-validation/tests/validation-machine.test.ts` "necessity evidence"
  drift: three `docs/.../*-v1.md` evidence docs pin an older blueprint digest
  and compiler build; the pins need refreshing against the live blueprint.
- `midgard-node/tests/published-workflow-deployment.test.ts` (uncommitted
  wave file, not an audit row): its helper requires `MIDGARD_REAL_BLUEPRINT_PATH`
  with no fallback, unlike every other consumer, so it fails at `beforeAll`
  on a machine without the env var set.
- `midgard-fault-proofs` direct-frontier-exact +1 refusal (see open item 1).
- `midgard-watcher` (uncommitted wave files, none are audit rows):
  `runtime/deployment-identity.test.ts` (26 tests, `unknown_field` at
  `releaseBindings.artifacts.fundingProfileBundleDigest` and
  `canonical_manifest_invalid`), `verification/rule-bundle.test.ts` (8 tests,
  same manifest drift), `fault-proofs/watcher-installed-journey.test.ts`
  (needs `MIDGARD_REAL_BLUEPRINT_PATH`), `l1/native-chain-sync.test.ts`
  (one identity assertion). `runtime/user-event-runtime.test.ts` failed only
  under full-suite load and passes alone. The six watcher files this
  remediation touched pass (65 tests).

## Incident during the run

One agent restored a mutated production file from `HEAD` instead of from a
pre-mutation backup and thereby discarded the uncommitted wave content of
`demo/midgard-fault-proofs/src/remove-fraudulent-block.ts`. Another agent
caught it and preserved the wave version, which was restored and verified
(typecheck clean, 55 tests green). Codex session logs were used to confirm no
other file was affected. A `git stash create` snapshot of the whole working
tree (stash entry "wave-snapshot 2026-09-09 22:30", tree untouched) was taken
as a safety net for the remaining batches.
