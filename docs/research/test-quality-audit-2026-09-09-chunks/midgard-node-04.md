# midgard-node-04

## Rows
./demo/midgard-node/tests/native-transaction-integration.test.ts	3833	9	A	good-integration;good-refusal-pairing;good-boundary;weak-assert	no	keep	Phase A/B native-transaction admission accepts canonical Midgard txs and refuses each specific malformation (aux data, bad signature, duplicate inputs/observers, extraneous witnesses, malformed program envelopes, legacy Cardano payloads) with the exact reject code	L2167;L2286;L2313;L2530;L2576 assert only detail.toBeTruthy() where the other ~74 cases assert an exact detail substring	-	-
./demo/midgard-node/tests/commit-end-time.test.ts	400	9	A	good-boundary;good-refusal-pairing;good-property	no	keep	Commit end-time resolution stays monotonic and slot-aligned under wall-clock jumps and reports fits/exceeds_cap against the scheduler window	-	-	-
./demo/midgard-node/tests/sdk-aiken-schema-parity.test.ts	404	8	B	external-contract-vector;source-text-oracle;good-refusal-pairing	no	strengthen	SDK Plutus schemas structurally equal the Aiken blueprint definitions they mirror, and native validity codes bind to their exact Plutus CBOR	L323-L353 proves absence of retired tx-order surfaces by regex over .ak/.ts source text and existsSync on paths - a static-text oracle presented alongside behavioural rows	-	Split the retired-surface check out and report it as a static architecture gate; the ABI rows already derive from the CI-rebuilt blueprint and need no pin
./demo/midgard-node/tests/database-pool-timeout.test.ts	41	6	C	self-pin;good-boundary	yes	strengthen	Pool connect timeout is 10s for every role and DatabaseError formatting preserves the nested cause chain and codes without leaking statement parameters	L15-L20 pins Duration.toMillis(databaseConnectTimeout(role)) === 10_000 against a literal copied from the production constant; an intentional retune must be hand-edited here and nothing independent justifies 10s	Catches a silent per-role divergence (one pool losing its timeout or getting a different one) and any regression that starts embedding SQL text in operator-facing error strings	Derive the role table from the production role union and assert role-invariance (all roles equal) plus a sane bound; keep the format assertion, whose oracle is independent
./demo/midgard-node/tests/mpf.test.ts	4144	9	A	good-integration;good-property;good-refusal-pairing;impl-coupled	no	keep	The Midgard MPF wrapper produces canonical roots and proofs, keeps durable Level state unchanged until an authenticated promotion, and fails closed on corrupt nodes, cap overruns, stale parked overlays and cyclic/duplicate mempool ordering	L1033;L1191-L1193;L1211-L1227;L2196-L2198 assert exact diagnostics counters (uniquePaths===5, retainedUpperNodes<=273, arenaCheckpointCalls, incrementalBranchDigests===updates*5) that a behaviour-preserving traversal change would move	Those counters are the memory/IO acceptance criteria for the arena engines - without them a regression that silently hydrates the whole trie would still pass on roots alone	Expose the bounds as production-side invariants asserted inside the arena (it already poisons on cap overrun) and assert only the bound here, not the exact count
./demo/midgard-node/tests/commit-block-header-state-queue-tail.test.ts	427	9	A	good-refusal-pairing;good-boundary;external-contract-vector	no	keep	Commit base resolution looks up only the exact expected state-queue NFT unit and fails closed on zero/multiple/advanced tails; the header end_time equals validTo-1 exactly, not merely inside the interval	-	-	-
./demo/midgard-node/tests/da-libp2p-runtime-manifest.test.ts	430	8	B	good-integration;good-refusal-pairing;weak-assert	no	keep	DA libp2p runtime manifests carry profile-correct multiaddrs and deployment identity, persist atomically, and are refused when schemaVersion, fingerprint, audit sha or deployment step status is wrong	L114;L163 assert only JSON.stringify(manifest).toContain(host) for the profile claim, though L127-L139 re-checks the parsed multiaddr	-	-
./demo/midgard-node/tests/deposit-flow-emulator-submission.test.ts	434	9	A	good-integration;good-boundary	no	keep	A deposit submitted on the emulator is projected, committed, confirmed and locally finalized with the exact DB statuses, DA payload row and header roots the block carries, and the local block boundary survives a cold start	L248-L254 computes expectedDepositsRoot with the same production buildAuthenticatedRootFromEncodedEntries the committer uses (helper at tests/deposit-flow-emulator-shared.ts:2570), so the root assertion checks header-vs-DB agreement rather than the root algorithm	-	-
./demo/midgard-node/tests/midgard-contracts.test.ts	434	5	C	self-pin;weak-assert;good-refusal-pairing	yes	strengthen	Real (non-placeholder) validators resolve for every protocol role and a deployment manifest that disagrees with config or has been tampered with is refused	L113-L146 pins ~16 script-title string constants against literal copies of those same constants (a wrong title already fails resolution, so the pin only fires when someone edits the constant); L218-L233 pins hand-maintained step/final counts 4/8/5/3/8 with a comment admitting the pin had lagged real additions; L148-L253 uses not.toEqual(placeholder) about 25 times, which any non-placeholder value satisfies	Catches an accidental fallback to the always-succeeds placeholder for one role and a lost fraud-proof step wiring	Derive titles and step counts from the blueprint validator list and the canonical manifest role map at test time; assert resolved script hashes equal blueprint-derived hashes instead of asserting inequality with the placeholder
./demo/midgard-node/tests/availability-challenge-operation.test.ts	435	10	A	good-integration;good-refusal-pairing;good-property	no	keep	Signed DA availability intents are durably journaled so a crash after broadcast replays the identical bytes, expiry needs positive unspent evidence, children expire after parents, and a rolled-back finalized anchor halts the actor across a restart	-	-	-
./demo/midgard-node/tests/speculative-commit-planner.test.ts	439	8	B	good-boundary;good-refusal-pairing;weak-assert	no	keep	The speculative-commit reducer makes the documented T1-T7 invalidation, rebuild-budget and submit-on-confirm transitions and degrades back to the legacy commit path	L249-L290 asserts only not.toThrow over the state x event product, a totality claim with no outcome oracle	-	-
./demo/midgard-node/tests/da-publication-reconciler-e2e.test.ts	450	7	B	env-gated;good-integration;good-refusal-pairing	no	wire-into-ci	The real DA publication reconciler converges durable per-peer delivery after a committee member restart, fences a stale completion token, and reaches zero backlog with zero conflicts	L42;L56 describe.skipIf(!enabled) on MIDGARD_RUN_DA_PHASE5_JOINED_E2E=1 - the file silently reports as skipped in the required midgard-node suite and only runs under the opt-in test:da-canonical-v1-e2e script, so its content protects nothing on the merge path	-	Give the joined E2E its own required CI job (real libp2p nodes on loopback plus the worker Postgres shard already exist in CI), or fail the opt-in wrapper when the env var is unset instead of skipping
./demo/midgard-node/scripts/phase3-architecture-g-closure.test.mjs	458	7	B	good-refusal-pairing;negative-uncontrolled;fixture-hides-decision	no	strengthen	The three Phase 3 report verifiers accept a well-formed final-tree/release-image/live-E2E report and reject missing suites, command drift, compiler residue, symbol tables, SHA drift, secret-scan failures, replay drift and noncanonical shapes	L351-L366;L377-L398;L409-L456 assert only passed===false per mutation and never which reason fired, so a mutation could be rejected by an unrelated rule; the accept fixtures at L114;L311 are built from the verifiers own exported PHASE3_FINAL_TREE_SUITES and PHASE3_LIVE_STEP_IDS, so the exact-set claim in both accept test names cannot fail when that required set itself changes	-	Assert the expected reason string per mutation case, and pin the required suite/step id sets against an independent source (the runner argv or the phase-3 spec doc) rather than re-reading the verifier's own export
./demo/midgard-node/tests/admission-writer-pipeline.test.ts	466	9	A	good-property;good-boundary;good-refusal-pairing	no	keep	The two-lane admission writer persists deterministic FIFO lanes concurrently, keeps collecting while a persister is blocked, backpressures on global capacity, releases capacity on a cancelled pre-permit waiter, and reports mayHaveCommitted correctly per shutdown stage	-	-	-

## Notes
- Production paths exercised: real Lucid Emulator (commit-end-time, deposit-flow-emulator-submission, availability-challenge-operation), real Postgres worker shard (deposit-flow-emulator-submission, da-publication-reconciler-e2e), real LevelDB + real @aiken-lang/merkle-patricia-forestry (mpf), real libp2p nodes on loopback ports (da-publication-reconciler-e2e), real UPLC evaluation of blueprint-compiled scripts (native-transaction-integration), real sqlite journal reopened across restarts (availability-challenge-operation). Only three files are mock-dominant: commit-block-header-state-queue-tail (vi.fn Lucid provider, but the resolution decision itself is real), speculative-commit-planner (pure reducer), admission-writer-pipeline (injected persister by design, and it claims pipeline mechanics only).
- Opt-in / CI gaps: `da-publication-reconciler-e2e.test.ts` is the only env-gated file in the chunk; it is also listed in `vitest.config.ts`'s Postgres inventory as "(opt-in)". It is the chunk's strongest evidence for durable DA delivery and it does not run on the merge path.
- `sdk-aiken-schema-parity.test.ts` carries a 70-line header (L46-L115) declaring 11 rows KNOWN RED. That redness is against a locally frozen `onchain/aiken/plutus.json`; the file is gitignored and `midgard-node-ci.yml` runs `aiken build --env testnet` (L187-L189) before the suite, so in CI the comparison is against a freshly generated blueprint and the file is green. The header's own measurement notes confirm this. Treat the red rows as a local-workflow artifact, not a broken required gate.
- Shared helpers the chunk's verdicts depend on: `tests/deposit-flow-emulator-shared.ts` (2,500+ lines; `expectedAuthenticatedEventRoot` at L2570 re-uses the production root builder, so root assertions are agreement checks, not independent oracles), `tests/helpers/availability-challenge.ts` and `tests/helpers/cardano-protocol-parameters.ts` (shared identity fixtures for both manifest files), `tests/midgard-output-helpers.ts` (CML output construction for mpf and native-tx files), `tests/utils.ts` (`provideDatabaseLayers`).
- Duplicated setup, not duplicated coverage: `midgard-contracts.test.ts` and `da-libp2p-runtime-manifest.test.ts` build essentially the same finalized deployment manifest (identical `DeploymentManifestIdentityContext` blocks, ~40 lines each) but assert different contracts (config/tamper refusal vs runtime-manifest generation). The manifest fixture builder is worth extracting; the tests are not worth merging.
- `phase3-architecture-g-closure.test.mjs` is the chunk's only node:test file; per the CI summary it is invoked directly by a CI step (not through vitest), so its node:test bodies do execute.
- Could not determine: whether the 273-node and 4,096-key bounds in `mpf.test.ts` (L1191, L1035, L1062) have a written derivation anywhere - they read as hand-computed frontier bounds, and I found no comment or doc deriving them.
