# Native script decoding installed workflow closure

Retain the six physical validators and their existing parameterization. Install
the shared cursor runner over Init, source bind, committed event bind, subject
opening, descriptor binding, repeated bounded scans, terminal mint and shared
removal. Capture each signed transaction before broadcast and persist its exact
intent in the central journal. Authenticated L1 datum checkpoints choose scan
segments after restart; no caller-supplied verdict or completion callback is an
authority.

Replay uses authenticated retained current and predecessor payload envelopes.
Reconstruct each prior committed ledger effect and verify every intermediate
root, then authenticate the selected descriptor under the exact event pre-root.
Resolve raw script bytes from retained canonical outputs and verify their
bounded-item commitment. Preserve submitted full transaction bytes, deriving
committed validity only when opening the transaction source. Forced selection
binds exactly the committed reason source kind and input index. Accepted replay
scans both spending and reference fields. Honest opposite polarity yields no
finding. Artifact admission rederives these facts after JSON restart.

OpenSubject field publication/certification and accepted membership chunk
publication use shared durable prerequisite ports. Maximum scan-source and
membership evidence, both directions, honest refusal, cancellation, intent
recovery, permanent proof mint and removal must pass registered Lucid tests.
Record measured fit with the shared writer and current blueprint verifier.

## Verified installed closure

The exact native descriptor domain caps the reference-script item at 16,384
bytes. The installed actuator exercises that complete bound, including the
first-chunk Merkle proof, descriptor membership, machine refusal, permanent
proof token and shared registered removal. It also exercises canonical
wrongful rejection and malformed wrongful acceptance. Every stage restores
its artifact from the directory journal after the broadcast/receipt gap and
reconciles the exact successor transaction.

The checked-in ledger contains 45 complete signed transaction measurements,
including all six physical script publications and field-certificate minting
script publication in each polarity/source scenario. Largest signed size is
12,837 bytes; peak memory is 3,101,024 and peak CPU is 1,060,141,829. The minimum
publication reserve margin is 3,035 bytes. These rows measure the installed
workflow's maximum native item source; they do not replace the existing
family's separate adversarial membership and scan-machine assurance.

Regenerate from the repository root with the pinned normal testnet blueprint:

```sh
PATH=/home/gumbo/.local/share/pnpm:/home/gumbo/.nvm/versions/node/v22.22.2/bin:$PATH MIDGARD_WRITE_FIT_LEDGER=1 pnpm --dir demo/midgard-fault-proofs exec vitest run tests/native-script-decoding-installed-lifecycle.test.ts
PATH=/home/gumbo/.local/share/pnpm:/home/gumbo/.nvm/versions/node/v22.22.2/bin:$PATH pnpm --dir demo/midgard-fault-proofs exec vitest run tests/native-script-decoding-workflow-fit-ledger.test.ts
```

The retained reconstruction exception accepts only an outer-canonical output
with a structurally invalid native script, exact bounded output and script
commitments, an exact outpoint key/index, and the authenticated complete ledger
root. Candidate descriptors carry no independent authority. Canonically valid
outputs continue to use canonical derivation. The node retains resolved input
membership and created-output descriptor witnesses so recovery does not depend
on a caller supplying descriptor facts.
