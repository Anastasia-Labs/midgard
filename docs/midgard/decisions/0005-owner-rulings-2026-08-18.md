# 0005 — Owner rulings, 2026-08-18 session

Status: ACCEPTED by the Goal owner's 2026-08-18 direction; AMENDED by the 2026-08-23 owner ruling session (see "Amendments (2026-08-23)" below).

One consolidated record for the ruling set issued in the 2026-08-18 owner session. Each ruling is also recorded on its tracker lane (issue numbers cited); this file is the single durable index.

## R1 — C52: proof-transaction count cap confirmed

The in-flight 2026-08-18 ruling retiring the "aggregate script-execution floor" (N=2 minimum-sufficient derivation, `f45dbaed`) in favor of the `PROOF_TRANSACTION_COUNT_CAP = 5_000` upper bound is **confirmed as genuine owner direction** — the cap framing matches the real constraint (challenge-period completion, not capacity). Conditions carried with the confirmation:

- The ledger entry, C52 manifest-row update, and issue record for the ruling are owed by the lane implementing it (nothing in `GOAL_PROGRESS.md` or the manifest records it yet).
- The 5,000 value is **owner-asserted pending measurement**: no landed evidence measures how many proof transactions any real fault proof requires. C53's resolver proof-fit sweep is the natural supplier of a measured basis; revisit the constant when it exists.
- Unlike the retired derivation, the cap does not re-derive on a C70 snapshot change — this is accepted, recorded here so it is not later read as an oversight.

## R2 — The Q02 permissive-dispatch scanner: narrow the scanner, not the code

All four standing findings are measured benign shapes (refusing-`expect` catch-all; two diagnostic-label-only fallbacks; conservative absent-asset zero-default). Narrow the four rules to recognize exactly these shapes, each narrowing proven by hostile negative controls in the `d1a6bccc` style. No on-chain movement; may land ahead of the wave. Recorded on #607 (its disposition lane); clears one of #614's two residual reds.

## R3 — #597 / C21 ruling (a): observe-stage reference script, inside the wave

The observe stage of `submitValidationDisputeSemanticResolution` gets a deployment-info role + publication step for `canonical_decode_item_observe_v1` (mirroring `validationTraceDisputeItemSemantic`) and sources it by reference. This rides the #617 batched regeneration wave as its lead item. **Precondition before any wave work:** measure that the ~11,097-byte observe body fits a real signed publication transaction (the C28 CEK-resolver precedent forbids assuming it); escalate if not.

Interim exposure accepted: the (≈5,287, 8,273] direct-route band remains unsubmittable until the wave lands, in lieu of a double rebind. If the wave slips materially, revisit the one-constant interim lowering to ~5,287.

## R4 — #597 / C21 ruling (b): the carriage bound is re-derived post-wave, owner signs the number

`maxReliableDirectCompleteItemBytes` (8,273) is not rebound now. After the wave's wiring lands, the lane measures the post-wiring direct-route frontier and **brings the proposed value to the owner for sign-off before it lands**. No unilateral rebind, in either direction.

**FULFILLED (2026-08-19 session continuation).** R3's wiring executed immediately at owner direction and surfaced a second embedded script of the same class (the prepare-resolver validator, 5,606 bytes inline in the prepare-selected witness set), which was wired by the same ruling. Measured end-to-end frontiers after both wirings (binder: authenticate): exact 13,294 / reserve 12,810. **Owner signed 12,810** (reserve policy). The (≈5,287, 8,273] liveness band closed at the first wiring; the interim-exposure acceptance in R3 is moot. Wiring commits on the branch: 8fe3052e (observe), 016682e7 (prepare-resolver). The `PrepareSelectedByEvidenceHash` arm-steering option (would make the step transaction item-size-independent at ~7.4K) remains a flagged, un-ruled owner option — on-chain semantics, wave-scoped to verify if ever taken.

## R5 — The regeneration wave: one batch, one cascade, one re-freeze (#617; AMENDED 2026-08-23 — see Amendments below)

All pending ABI-touching changes travel together: the observe role (R3), the Cek + ValueAndMint direct-resolver splits (closes the CG3 totality gaps at indices 11/12), #606's pexcludes certificate fold, the `no-input/step-04.ak` terminal-arg normalization (R7), and the E_MIN_ADA on-chain wiring (R8). Then the full §11 IG1 cascade, blueprint regeneration in the parent lane, §3.2 necessity + C74 remeasurement, and a single re-freeze re-pinning everything stamped PRE_FREEZE. No ABI-touching change lands outside the wave.

## R6 — #611: the tier-1 preimage cap is repriced, not sub-ranged

The falsified 14,336-byte cap will not be kept as a documented-sub-range fiction. The definitive value comes from the wave's necessity remeasurement; the measured reserved frontier comes to the owner for sign-off before rebinding (same protocol as R4). FALSIFIED annotations stay until then.

## R7 — #607: accepted; step-04 normalized in the wave (AMENDED 2026-08-23 — see Amendments below)

The generator work is accepted. The shipped `no-input/step-04.ak` terminal-arg inconsistency is normalized onto the standard pattern inside the wave (the sanctioned ABI-movement moment), after which the generator covers every family with no per-family exceptions.

## R8 — Min-Ada (Q27 lineage): wiring, scope, and D-S4 (#618)

Completing decision 0003:

1. The E_MIN_ADA wiring lands in the C42/C49 lane now, not in Q27; zero-lovelace fixtures are re-authored, not suppressed. Its on-chain half batches into the wave.
2. The C42 manifest contradiction (green pinned counts vs absent prescribed rejection-code evidence) is repaired in the same pass.
3. Q27 covers **both** violation shapes — MIN-ADA-TX and MIN-ADA-UTXO — recorded in its manifest row.
4. D-S4 closes: the implemented formula (`coins_per_utxo_byte × (160 + serialized canonical output bytes)`) is written into the technical spec with the rate cited from the pinned C70 snapshot; a cross-check binds the Aiken and TypeScript implementations; the stale CML comment is fixed.

## R9 — Issue dispositions

- **#614** — stays open until the first fully green Midgard Node CI run; owns the accepted-red-map downward re-pin and absorbs #616 (the timeout flake: bump per precedent, never absorb into the accepted baseline).
- **#611** — open pending R6's sign-off number.
- **#610** — stays open for the addendum's completion: extend the arity-door scan to `demo/midgard-fault-proofs` and guard both bare `compiledCode` loaders. Wave-independent.
- **#607** — open pinned to R2 + R7; close when both land.
- **#590** — CLOSED as completed-by-#592; the one inline-index residue is loud-failing and derived on next touch.
- **#582** — CLOSED, `needs-triage` dropped; its disclaimed residual is now #615 (no-reference-input zero-test coverage gap).
- **#486** — C30–C33 promoted PARTIAL→PASS on the recorded measurements (the C32 row repair was transparent and attributed; no replay required). CG2 follows the wave + R4 sign-off.
- **#479** — C10–C13 accepted now; the consolidated CG1 review happens once, post-wave, against final hashes. Open only for that owner event.

## New tickets from this session

#615 (no-reference-input coverage gap), #616 (submit-init timeout flake), #617 (the regeneration wave), #618 (E_MIN_ADA wiring + D-S4).

## Amendments (2026-08-23)

A second owner ruling session, held while the #617 wave was staged and before its single regeneration, extended this record. Each amendment is recorded as an **OWNER RULING (2026-08-23)** comment on its own tracker lane; the operative text is quoted below so this file carries its own provenance. No ruled text above is rewritten — R5's contents and R7's scope grow, and the growth is stated here.

### A1 — R7 covers four terminal modules, and R5's R7 item grows with it (#626, owner comment of 2026-08-23)

R7 said the Q02 generator would cover every family with no per-family exception once `no-input/step-04.ak` was normalized. The terminal-args sweep that landed alongside it measured three more shipped terminals still trailing `fraud_proof_mint_redeemer_index` behind their family-specific arguments, and carried them as a pinned exception list under #626. Those three are now ruled into the wave:

> **Ruled in.** The three terminal reorders join the wave under R5 and extend R7: `invalid-signature/step-02`, `no-reference-input/step-04`, `withdrawn-reference-input/step-03` take the shipped `input_index, output_index, fraud_proof_mint_redeemer_index, <family args>` order, so "no per-family exceptions" is true as measured. Re-measured at parent HEAD: three scripts, three definitions, nothing else. Land before the single regeneration, as one commit — both on-chain sites, both SDK schemas, both submitters — since no gate catches a half-applied reorder of same-typed `Int` fields. Delete the exception list and its size pin with it. Use the rebased patch; the staged one predates 1ac7d5b7. Amend decision 0005 R5/R7 in the same pass. Closes #626; #607 closes with it.

R7 therefore names four normalized modules — `no-input/step-04.ak`, `invalid-signature/step-02.ak`, `no-reference-input/step-04.ak` and `withdrawn-reference-input/step-03.ak` — and R5's fourth content item, "the `no-input/step-04.ak` terminal-arg normalization (R7)", reads as all four. R9 keeps #607 open pinned to R2 + R7; this reorder is the R7 half, and the ruling closes #607 with #626.

### A2 — R5 contents, items 6 to 8 (#625, #628, #629)

R5 enumerated five members of the wave. Three more were ruled into it in the same session. All three are ABI-touching and so bound by R5's "No ABI-touching change lands outside the wave"; none changes the shape of the §11 IG1 cascade, only the tree it runs over.

**Item 6 — the MPF sentinel repair (#625, owner comment of 2026-08-23).** Not a new decision: the repair was approved on 2026-08-17 and pinned to #510's single regeneration, and #617 is that event.

> Ruled: fold it in. Not a new decision — the repair was approved 2026-08-17 and pinned to #510's single regeneration; #617 is that event. R5's contents are amended to add it as item 6. Scope is the library, not the call sites: translate the sentinel in `mpf_proof_v1.does_not_have` **and** fold the same translation into `insert_root`, which becomes a live abort path the moment the gate opens. Both, or neither. Evidence: mirror pexcludes' three selectors — sentinel positive, populated-trie positive, unproven-root negative — and clear the residual notes in `mpf-proof-v1.ak`, `chunked-inclusion-v1.ak` and `docs/public_testnet_readiness.md:52` in the same pass. No spec text moves.

**Item 7 — the `utxos_root` descriptor repoint (#628, owner comment of 2026-08-23).** Escalated from the wave lane on 2026-08-22 with its severity corrected: a live unilateral-slashing hole rather than a liveness gap, so deferral past the wave was not an option.

> The descriptor is the sole `utxos_root` trie value — the spec says so four times, once explicitly excluding full output bytes, and 7a87a5402 moved everything except `transition-trace/proof.ak`. That file is stale, not authoritative. This is worse than reported: `apply_l2_outputs` and `validate_valid_deposit_transition` let any challenger mint a fraud-proof token against an honest block, since the full-output replay can never equal a descriptor-built root. Both arms repoint onto `LedgerOutputCommitmentV1`, with an on-chain `output_cbor → descriptor` builder and measured ExUnits. Rides #617's single regeneration — `accepted_transaction_v1` and `deposit_v1` move. Update `proof.test.ak`, `detect.ts:1439` and the challenger fixture's full-output ledger trie in the same pass. No deletion of the L2 arm.

**Item 8 — the cek hand-off discriminator (#629, owner comment of 2026-08-23).** Option (A) of the two prepared in the wave lane, with its ABI movement measured before the ruling: the two moving hashes are cek semantic resolvers the wave already re-pins.

> **#629 — option (A), inside the wave.** The cek execution ladder walks every script execution; native entries are ordinary selections that authenticate their frontier entry and advance the cursor. The language bitmap records Plutus languages for script-integrity and takes no part in discriminating the hand-off — the cursor alone does. The on-chain shortcut is the defect: it let a control assert an empty bitmap and claim the hand-off without proving anything about the execution frontier, and contradicted the machine's own behaviour for mixed transactions. Rides #617's single regeneration; the two moving cek semantic hashes are already in its re-pin set. No trace, work root or pinned fixture moves. Add the ladder rule to the consensus profile's cek phase text in the same pass.

### A3 — R5 contents, item 9 (#633, owner direction of 2026-08-23)

#633 asked which decoder is authoritative for tag-0 (`NativeCardano`) reference-script bytes. The ruling is **structural**: spec §5.3 says tag-0 `script_bytes` "carries the canonical Midgard native-script CBOR" and §6.1 makes decoders fail closed on everything else; the staged on-chain admission machine already enforces it (`ledger_output_proof_v1` runs `native_script_scan_v1` over every tag-0 reference script and renders `E_INVALID_FIELD_TYPE`); the TypeScript codec enforces it; Cardano L1 structurally decodes native scripts. The deviant is the one-shot `ledger_output_v1.parse_script_ref`, which is opaque for tag 0 — an *intra-on-chain* inconsistency between the staged machine and the one-shot descriptor path, made newly reachable by item 7's repoint. Admissibility is settled with it: a transaction carrying such an output is not admissible as a valid (code-0) transaction in `transactions_root`; an honest node already rejects it at Phase A, and a forced-inclusion copy commits only under a `Rejected` verdict. #633's issue text said "not a #617 rider" because the wave looked sealed; with the wave still in flight and item 7 already moving `accepted_transaction_v1` and `deposit_v1` — the same hashes this fix moves — the owner directed it in.

> **#633 — inside the wave, as item 9.** `ledger_output_v1.parse_script_ref`'s tag-0 arm gains the canonical native-script structural predicate, under the **structure-scan bounds** (16,384 nodes / 16,384 depth, `native_script_scan_v1`'s — not the 16/32 evaluator bounds, or the one-shot would reject outputs the staged machine admits). Precondition in the R3 style: measure ExUnits on the `accepted_transaction_v1` arm's worst case (maximally nested tag-0 payloads inside the 32,768-byte field bound) before commitment; if the budget breaks, the item drops out of the wave and #633's on-chain half queues for the next regeneration instead — escalate, never trim the bounds unilaterally. In the same item: repair the false totality claim in `ledger-output-descriptor-v1.ak`'s `build_v1` doc comment ("exactly the set the off-chain encoder also refuses" — false while tag-0 is opaque), and land the probe vector red→green (`a300581d60` ‖ `aa`×28 ‖ `018200a003820043deadff` → `decode_canonical_output == None`). The blueprint diff records the moved-hash set; runtime reach is `accepted_transaction_v1`'s arm only (`deposit_v1` builds its projected output with `script_ref: None`, `transition-trace/proof.ak:901` — its hash moves, its behaviour does not). Wave-independent halves land outside the wave: the spec §5.5 clarifying sentence, the TypeScript pin vectors, and the challenger-liveness repair (the reference challenger currently cannot prove *any* fraud in a tx carrying an output item its codec refuses — filed as its own issue; not tag-0-specific).
