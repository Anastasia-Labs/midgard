---
title: "CLI Reference"
description: "The midgard-node command-line interface, grouped by function."
---

`midgard-node` is a Commander CLI (`bin: midgard-node`). Run `midgard-node --help`
for the authoritative, version-specific list. The commands below are grouped by
function; names are taken from `src/index.ts` on the `tx-validation` branch.

<Callout type="info" title="The server">
`listen` is the long-running command that starts the HTTP API server and the
[background fibers](/docs/operators/node/background-fibers). See
[Server &amp; HTTP API](/docs/operators/node/server-http-api).
</Callout>

## Initialization & deployment

`init` · `export-contract-deployment-info` · `deployment-manifest` ·
`deployment-status` · `reference-scripts-complete` ·
`reference-script-wallet-status` · `sweep-reference-script-wallet`

## Database

`db:migrate` · `db:status` · `db:verify` · `db:checksum` ·
`db:backfill-da-payloads`, see [Database &amp; State](/docs/operators/node/database).

## Operators & PHAS

`register-active-operator` · `activate-operator` ·
`register-phas-membership-reward-account` · `phas-registered`

## L2 transactions, deposits & withdrawals

`submit-l2-transfer` · `submit-deposit` · `submit-withdrawal` ·
`withdrawal-status` · `project-deposits-once` · `deposit-projected` ·
`fetch-withdrawals-once` · `absorb-confirmed-deposit-to-reserve` ·
`reconcile-deposit-submission`, see
[Deposits &amp; Withdrawals](/docs/operators/node/deposits-withdrawals).

## Blocks, attestation & settlement

`commit-explicit-block-header` · `block-committed` · `tx-committed` ·
`attest-state-queue-once` · `da-attested` · `merge-complete` ·
`resolve-event-settlement-proof` · `reconcile`

## Reserve & payout

`initialize-payout` · `add-reserve-funds-to-payout` · `conclude-payout` ·
`payout-status` · `reserve-utxos`, see
[Reserve &amp; Payout](/docs/operators/node/reserve-payout).

## Hub oracle & wallets

`prepare-hub-oracle-one-shot-nonce` · `create-l2-wallet` · `address-from-seed` ·
`utxos` · `l1-utxos` · `l1-provider-preflight`

## Audit, DA transport & e2e

`audit-blocks-immutable` · `da-libp2p-preflight` · `da-libp2p-generate-manifest` ·
`e2e-start-service` · `e2e-run-step` · `e2e-finalize-summary` ·
`e2e-stress-l2-throughput` · `stress-wallets:prepare`, see
[Benchmarks &amp; Stress Testing](/docs/operators/node/benchmarks).
