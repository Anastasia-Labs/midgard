apiVersion: 1

providers:
  - name: Default # A uniquely identifiable name for the provider
    folder: Services # The folder where to place the dashboards
    type: file
    options:
      path: DASHBOARD_PATH_PLACEHOLDER
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 10

