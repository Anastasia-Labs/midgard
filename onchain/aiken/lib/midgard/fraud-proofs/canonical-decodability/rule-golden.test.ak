//// Generated by
//// demo/midgard-sdk/scripts/generate-canonical-decodability-v1-goldens.mjs.
//// Do not edit; regenerate from the TypeScript twins.
////
//// The §12.7 cross-language goldens. Two sets, and they answer different
//// questions.
////
//// **The verdict set** is the one that matters. §12.7's verdict is a total
//// decision procedure implemented twice — here and in
//// `demo/midgard-sdk/src/fraud-proof/canonical-decodability-v1.ts` — and the
//// two must agree on every byte string. A disagreement is silent in both
//// directions: a twin that calls a committed non-envelope grammatical leaves
//// a genuine fault unfiled, and one that calls an envelope ungrammatical has
//// a prover spend a transaction on a step step 02 refuses. Every vector below
//// carries the code the TypeScript twin assigned it, and the test asserts the
//// Aiken verdict is the same number.
////
//// **The wire set** round-trips the two Data-encoded surfaces the family
//// adds: each test decodes the TypeScript producer's bytes into the Aiken
//// type and then re-serialises what came back, so a decoder that accepted a
//// shape the producer never emits fails the second half and an encoder that
//// emitted a shape the decoder tolerates fails it too. `CommittedFieldClaimV1`
//// constructor order is wire format: `BodyFieldClaim` 0, `WitnessFieldClaim` 1.

use aiken/cbor
use midgard/fraud_proofs/canonical_decodability/rule.{
  BodyFieldClaim, CommittedFieldClaimV1, WitnessFieldClaim, envelope_verdict_v1,
}
use midgard/fraud_proofs/canonical_decodability/step_02.{State}
use midgard/fraud_proofs/native_tx/types.{NativeTxWitnessSetCompact}
use midgard/native_tx_field_access_v1.{Certified, Inline, RawUtxo}

// ---------------------------------------------------------------------------
// Verdict vectors
// ---------------------------------------------------------------------------

/// `empty_field` — §5.1's empty field, and the shortest admissible
/// preimage there is. 1 bytes, verdict 0 (`grammatical`).
test canonical_decodability_golden_verdict_empty_field() {
  envelope_verdict_v1(#"80") == 0
}

/// `one_item` — the ordinary shape. 6 bytes, verdict 0 (`grammatical`).
test canonical_decodability_golden_verdict_one_item() {
  envelope_verdict_v1(#"8144deadbeef") == 0
}

/// `twenty_three_items` — the widest one-byte array header, `97`. 47
/// bytes, verdict 0 (`grammatical`).
test canonical_decodability_golden_verdict_twenty_three_items() {
  envelope_verdict_v1(
    #"9741004100410041004100410041004100410041004100410041004100410041004100410041004100410041004100",
  ) == 0
}

/// `twenty_four_items` — the narrowest two-byte array header, `98 18`. 50
/// bytes, verdict 0 (`grammatical`).
test canonical_decodability_golden_verdict_twenty_four_items() {
  envelope_verdict_v1(
    #"9818410041004100410041004100410041004100410041004100410041004100410041004100410041004100410041004100",
  ) == 0
}

/// `two_hundred_fifty_six_items` — the narrowest three-byte array header,
/// `99 0100`. 515 bytes, verdict 0 (`grammatical`).
test canonical_decodability_golden_verdict_two_hundred_fifty_six_items() {
  envelope_verdict_v1(
    #"",
  ) == 0
}

/// `twenty_four_byte_item` — the narrowest two-byte item header, `58 18`.
/// 27 bytes, verdict 0 (`grammatical`).
test canonical_decodability_golden_verdict_twenty_four_byte_item() {
  envelope_verdict_v1(#"8158185a5a5a5a5a5a5a5a5a5a5a5a5a5a5a5a5a5a5a5a5a5a5a5a") == 0
}

/// `two_hundred_fifty_six_byte_item` — the narrowest three-byte item
/// header, `59 0100`. 260 bytes, verdict 0 (`grammatical`).
test canonical_decodability_golden_verdict_two_hundred_fifty_six_byte_item() {
  envelope_verdict_v1(
    #"",
  ) == 0
}

/// `no_bytes` — one byte short of the empty field. 0 bytes, verdict 1
/// (`missing_array_header`).
test canonical_decodability_golden_verdict_no_bytes() {
  envelope_verdict_v1(#"") == 1
}

/// `four_byte_array_head` — well-formed CBOR, outside §5.1's acceptance
/// set. 5 bytes, verdict 2 (`not_an_array_header`).
test canonical_decodability_golden_verdict_four_byte_array_head() {
  envelope_verdict_v1(#"9a00000001") == 2
}

/// `four_byte_item_head` — the same exclusion at the item head. 7 bytes,
/// verdict 6 (`not_an_item_header`).
test canonical_decodability_golden_verdict_four_byte_item_head() {
  envelope_verdict_v1(#"815a0000000100") == 6
}

/// `non_minimal_array_head_at_23` — 23 items spelled in the two-byte
/// form. 2 bytes, verdict 3 (`non_minimal_array_header`).
test canonical_decodability_golden_verdict_non_minimal_array_head_at_23() {
  envelope_verdict_v1(#"9817") == 3
}

/// `non_minimal_array_head_at_255` — 255 items spelled in the three-byte
/// form. 3 bytes, verdict 3 (`non_minimal_array_header`).
test canonical_decodability_golden_verdict_non_minimal_array_head_at_255() {
  envelope_verdict_v1(#"9900ff") == 3
}

/// `truncated_array_head_two_byte` — a `98` head with no count byte. 1
/// bytes, verdict 4 (`truncated_array_header`).
test canonical_decodability_golden_verdict_truncated_array_head_two_byte() {
  envelope_verdict_v1(#"98") == 4
}

/// `truncated_array_head_three_byte` — a `99` head with one of its two
/// count bytes. 2 bytes, verdict 4 (`truncated_array_header`).
test canonical_decodability_golden_verdict_truncated_array_head_three_byte() {
  envelope_verdict_v1(#"9900") == 4
}

/// `declares_more_items_than_it_carries` — the walk runs out of preimage
/// before it runs out of declared items. 4 bytes, verdict 5
/// (`missing_item_header`).
test canonical_decodability_golden_verdict_declares_more_items_than_it_carries() {
  envelope_verdict_v1(#"8242dead") == 5
}

/// `declares_one_item_and_carries_none` — the same, at the smallest
/// cardinality there is. 1 bytes, verdict 5 (`missing_item_header`).
test canonical_decodability_golden_verdict_declares_one_item_and_carries_none() {
  envelope_verdict_v1(#"81") == 5
}

/// `item_head_wrong_major` — a CBOR uint where §5.1 requires a
/// byte-string head. 2 bytes, verdict 6 (`not_an_item_header`).
test canonical_decodability_golden_verdict_item_head_wrong_major() {
  envelope_verdict_v1(#"8100") == 6
}

/// `non_minimal_item_head_at_23` — a 23-byte item spelled in the two-byte
/// form. 3 bytes, verdict 7 (`non_minimal_item_header`).
test canonical_decodability_golden_verdict_non_minimal_item_head_at_23() {
  envelope_verdict_v1(#"815817") == 7
}

/// `non_minimal_item_head_at_255` — a 255-byte item spelled in the
/// three-byte form. 4 bytes, verdict 7 (`non_minimal_item_header`).
test canonical_decodability_golden_verdict_non_minimal_item_head_at_255() {
  envelope_verdict_v1(#"815900ff") == 7
}

/// `truncated_item_head_two_byte` — a `58` item head with no length byte.
/// 2 bytes, verdict 8 (`truncated_item_header`).
test canonical_decodability_golden_verdict_truncated_item_head_two_byte() {
  envelope_verdict_v1(#"8158") == 8
}

/// `truncated_item_head_three_byte` — a `59` item head with one of its
/// two length bytes. 3 bytes, verdict 8 (`truncated_item_header`).
test canonical_decodability_golden_verdict_truncated_item_head_three_byte() {
  envelope_verdict_v1(#"815901") == 8
}

/// `truncated_item_payload` — an item declaring two payload bytes with
/// one left. 3 bytes, verdict 9 (`truncated_item_payload`).
test canonical_decodability_golden_verdict_truncated_item_payload() {
  envelope_verdict_v1(#"8142ff") == 9
}

/// `truncated_item_payload_at_the_end` — a complete first item and a
/// truncated second — the walk gets past item 0 before it fails. 5 bytes,
/// verdict 10 (`trailing_bytes`).
test canonical_decodability_golden_verdict_truncated_item_payload_at_the_end() {
  envelope_verdict_v1(#"81410142ff") == 10
}

/// `declares_fewer_items_than_it_carries` — the preimage runs out of walk
/// before it runs out of bytes. 7 bytes, verdict 10 (`trailing_bytes`).
test canonical_decodability_golden_verdict_declares_fewer_items_than_it_carries() {
  envelope_verdict_v1(#"8142dead42beef") == 10
}

/// `trailing_byte_after_the_empty_field` — the smallest trailing-content
/// vector there is. 2 bytes, verdict 10 (`trailing_bytes`).
test canonical_decodability_golden_verdict_trailing_byte_after_the_empty_field() {
  envelope_verdict_v1(#"8041") == 10
}

// ---------------------------------------------------------------------------
// Wire vectors
// ---------------------------------------------------------------------------

const claim_body_inline_cbor: ByteArray = #"d8799f02d8799f428041ffff"

fn claim_body_inline_value() -> CommittedFieldClaimV1 {
  BodyFieldClaim { field_index: 2, carriage: Inline { preimage: #"8041" } }
}

/// CommittedFieldClaimV1 / `claim_body_inline`: decode the TypeScript
/// producer's bytes, then re-serialise what came back.
test canonical_decodability_golden_claim_body_inline_round_trips() {
  expect Some(decoded_data) = cbor.deserialise(claim_body_inline_cbor)
  expect decoded: CommittedFieldClaimV1 = decoded_data
  let rebuilt: Data = claim_body_inline_value()
  and {
    decoded == claim_body_inline_value(),
    cbor.serialise(rebuilt) == claim_body_inline_cbor,
  }
}

const claim_body_certified_cbor: ByteArray = #"d8799f00d87b9f029f030405ffffff"

fn claim_body_certified_value() -> CommittedFieldClaimV1 {
  BodyFieldClaim {
    field_index: 0,
    carriage: Certified {
      cert_ref_input_index: 2,
      chunk_ref_input_indices: [3, 4, 5],
    },
  }
}

/// CommittedFieldClaimV1 / `claim_body_certified`: decode the TypeScript
/// producer's bytes, then re-serialise what came back.
test canonical_decodability_golden_claim_body_certified_round_trips() {
  expect Some(decoded_data) = cbor.deserialise(claim_body_certified_cbor)
  expect decoded: CommittedFieldClaimV1 = decoded_data
  let rebuilt: Data = claim_body_certified_value()
  and {
    decoded == claim_body_certified_value(),
    cbor.serialise(rebuilt) == claim_body_certified_cbor,
  }
}

const claim_witness_raw_utxo_cbor: ByteArray =
  #"d87a9f06d8799f582031313131313131313131313131313131313131313131313131313131313131315820323232323232323232323232323232323232323232323232323232323232323258203333333333333333333333333333333333333333333333333333333333333333ffd87a9f02ffff"

fn claim_witness_raw_utxo_value() -> CommittedFieldClaimV1 {
  WitnessFieldClaim {
    field_index: 6,
    witness_set: NativeTxWitnessSetCompact {
      addr_tx_wits_hash: #"3131313131313131313131313131313131313131313131313131313131313131",
      script_tx_wits_hash: #"3232323232323232323232323232323232323232323232323232323232323232",
      redeemer_tx_wits_hash: #"3333333333333333333333333333333333333333333333333333333333333333",
    },
    carriage: RawUtxo { ref_input_index: 2 },
  }
}

/// CommittedFieldClaimV1 / `claim_witness_raw_utxo`: decode the TypeScript
/// producer's bytes, then re-serialise what came back.
test canonical_decodability_golden_claim_witness_raw_utxo_round_trips() {
  expect Some(decoded_data) = cbor.deserialise(claim_witness_raw_utxo_cbor)
  expect decoded: CommittedFieldClaimV1 = decoded_data
  let rebuilt: Data = claim_witness_raw_utxo_value()
  and {
    decoded == claim_witness_raw_utxo_value(),
    cbor.serialise(rebuilt) == claim_witness_raw_utxo_cbor,
  }
}

const state_convicting_cbor: ByteArray =
  #"d8799f58202222222222222222222222222222222222222222222222222222222222222222020aff"

fn state_convicting_value() -> State {
  State {
    bad_tx_id: #"2222222222222222222222222222222222222222222222222222222222222222",
    field_index: 2,
    verdict: 10,
  }
}

/// State / `state_convicting`: decode the TypeScript
/// producer's bytes, then re-serialise what came back.
test canonical_decodability_golden_state_convicting_round_trips() {
  expect Some(decoded_data) = cbor.deserialise(state_convicting_cbor)
  expect decoded: State = decoded_data
  let rebuilt: Data = state_convicting_value()
  and {
    decoded == state_convicting_value(),
    cbor.serialise(rebuilt) == state_convicting_cbor,
  }
}

const state_grammatical_cbor: ByteArray =
  #"d8799f582022222222222222222222222222222222222222222222222222222222222222220800ff"

fn state_grammatical_value() -> State {
  State {
    bad_tx_id: #"2222222222222222222222222222222222222222222222222222222222222222",
    field_index: 8,
    verdict: 0,
  }
}

/// State / `state_grammatical`: decode the TypeScript
/// producer's bytes, then re-serialise what came back.
test canonical_decodability_golden_state_grammatical_round_trips() {
  expect Some(decoded_data) = cbor.deserialise(state_grammatical_cbor)
  expect decoded: State = decoded_data
  let rebuilt: Data = state_grammatical_value()
  and {
    decoded == state_grammatical_value(),
    cbor.serialise(rebuilt) == state_grammatical_cbor,
  }
}
